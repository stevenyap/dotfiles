name: liwei-app
root: ~/Workspace/playground/liwei-app

pre_window: nvm use

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - server: print -S "npm start"
  - deploy: print -S "npm run deploy"
