name: hobnob-app
root: ~/Workspace/hobnob/app

pre_window: nvm use

windows:
  - editor: nvim
  - bash: git fetch --all && gh issue list --assignee @me && gh pr list --author @me && git status
  - cypress: print -S "make test-integration-local"
