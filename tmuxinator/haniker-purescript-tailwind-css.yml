name: haniker-purescript-tailwind-css
root: ~/Workspace/haniker/purescript-tailwind-css/

pre_window: nvm use

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - build: print -S "make build"
  - test: print -S "make run-test"
