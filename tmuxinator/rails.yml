name: rails
root: ~/Workspace/rails

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - test: tail -f log/test.log
  - console: history -s rails console
  - server: history -s rails server
