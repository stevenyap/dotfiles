name: chanceupon-web
root: ~/Workspace/chanceupon/web

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - ngrok: history -s "/Applications/ngrok http 5000"
  - server: history -s "yarn start"
